apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quartr.fullname" . }}-test-connection"
  labels:
    {{- include "quartr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quartr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
